# WebPredictorV2
This repository is intended to illustrate how to use develop a web using FastAPI as backend. This page allows you to predict tabular data either by calling the API or by using the web interface.  Additionally, it uses a relational database to control call credit and who uses the API.
